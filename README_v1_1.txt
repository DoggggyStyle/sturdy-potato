Ultimate Hacker — EA v1.1 (Text-Only Dialogue)
- Voice-over removed; dialogue is text-only with emotion-driven music cues.
- Includes all systems from EA v1: dynamic world, factions, festivals, base, mods/workshop, fashion visuals, layered music.

Quick Start
1) Open `project/` with Godot 4.x.
2) Run from editor or use `Export_Windows.bat` / `Export_Linux.sh` if present to build.
3) Dialogue system entry: `scripts/systems/TextOnlyDialogue.gd` + `EmotionMusicCue.gd` (already included).
